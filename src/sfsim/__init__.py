"""sfsim — a mini generative-agents simulation of San Francisco residents."""

from sfsim.constants import NUM_AGENTS


def main() -> None:
    """Entry point placeholder; the full pipeline is wired up in later phases."""
    print(f"sfsim ready — will simulate {NUM_AGENTS} SF residents.")
