"""Reflection step: after voting, each agent writes a one-sentence reflection.

The reflection nudges one OCEAN parameter (clamped to 1-10) and is stored in the
agent's memory for future scenarios (Part 4). Implemented in Phase 5.
"""
