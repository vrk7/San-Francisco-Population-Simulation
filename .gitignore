# Local reference / working docs — not part of the public deliverable
2304.03442v2.pdf
CLAUDE.md
PLAN.md

# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Secrets
.env

# Generated artifacts
results/

# Large raw PUMS downloads (regenerate via scripts/build_sf_pums.py).
# The slim data/sf_pums.csv IS committed so the repo runs without the ~360MB download.
data/*.zip
data/psam_*.csv
